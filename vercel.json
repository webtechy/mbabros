{
  "installCommand": "pnpm install --filter ./artifacts/cloud-consulting --frozen-lockfile",
  "buildCommand": "pnpm --dir artifacts/cloud-consulting run build",
  "outputDirectory": "artifacts/cloud-consulting/dist"
}